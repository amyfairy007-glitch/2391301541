# Mobile Upload Setup

Upload the contents of this package to the repository root.

After setup, use `docs/uploads/` as the default destination for new files created on a phone.
Reusable automation prompts should go into `docs/prompts/`.
