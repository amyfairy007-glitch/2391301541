请对当前新 Journey 的 Step 1～Step 9“成熟模板业务清除结果”进行一次只读验收。

## 一、任务背景

当前流程已经完成：

1. 基于成熟模板 Journey 创建新 Journey；
2. 对新 Journey 的 Step 1～Step 9 执行业务清除；
3. 下一阶段准备填充目标 Journey 自己的业务内容。

但当前业务清除实现没有充分基于 Step 1～Step 9 各自的真实调用边界进行清理，因此现有清除结果不能直接判定可靠。

本次任务只做验收和审计，不修改任何文件，不填充目标 Journey 业务。

## 二、任务目标

验证以下内容：

1. 成熟模板 Journey 在 Step 1～Step 9 中的业务内容是否已经清除；
2. 每一步的公共结构、调用链和产物关系是否完整保留；
3. 是否存在成熟模板业务残留；
4. 是否误删了公共逻辑；
5. 是否破坏了步骤之间的输入输出关系；
6. 当前新 Journey 是否可以作为后续九步业务填充的可靠底板。

## 三、重要原则

以下均不能直接当作事实：

* 每一步的公共代码范围；
* 每一步的 Journey 专属业务范围；
* 当前清除结果已经正确；
* Step 1～Step 9 可以使用同一种清除规则；
* 某些相似代码一定属于公共代码；
* 某些被删除代码一定属于业务代码。

必须以成熟模板真实代码、配置、调用链、步骤产物和清理前后 diff 为依据。

## 四、分析范围

请定位并对比：

* 成熟模板 Journey 的 Step 1～Step 9；
* 当前清理后的新 Journey Step 1～Step 9；
* 各步骤直接依赖的配置、数据、selector、helper、脚本和 Schema；
* 步骤之间传递的中间产物；
* 当前负责创建新 Journey 和清除业务内容的实现；
* 清理日志、报告或 diff，如存在。

只读取与九步流程和清除实现直接相关的文件，不全量扫描无关仓库内容。

## 五、先还原九步真实结构

请分别分析 Step 1～Step 9，不要只根据步骤名称猜职责。

对每一步确认：

| 项目         | 需要确认的内容            |
| ---------- | ------------------ |
| 正式入口       | 命令、文件、函数           |
| 输入         | 配置、文件、参数、上一步产物     |
| 核心执行       | 实际调用链              |
| 公共逻辑       | 可复用且不应清除的内容        |
| Journey 业务 | 与成熟模板业务绑定的内容       |
| 输出         | 文件、JSON、截图、报告或其他产物 |
| 下游         | 哪一步使用该产物           |
| 异常处理       | 失败方式和状态记录          |

输出每一步的真实调用链，例如：

```text
Step N 入口
→ 输入读取
→ 公共处理
→ Journey 专属处理
→ 产物生成
→ 输出给 Step N+1
```

以上结构仅为示例，必须以真实实现为准。

## 六、分别建立 Step 1～Step 9 的业务指纹

必须从成熟模板的真实实现中提取，不得只搜索 Journey 名称。

业务指纹可能包括：

* Journey 名称及变体；
* URL、菜单、页面标题；
* selector；
* 页面操作；
* 测试数据；
* 字段名及字段映射；
* API、请求参数、响应字段；
* validation 规则；
* action、场景、状态；
* 表格列、Tab、弹窗、iframe；
* 等待条件和成功判断；
* 截图名称；
* 输出目录或文件名；
* JSON 中的业务标识；
* 业务专属配置键；
* 业务辅助函数；
* 白名单、忽略项、映射规则；
* 模板 Journey 特有注释或说明。

请按步骤分别输出业务指纹，不能把九步混成一份泛化列表。

## 七、检查每一步的业务残留

在当前新 Journey 中逐步搜索成熟模板业务残留。

对每个残留分类为：

* 无残留；
* 可解释残留；
* 不可解释残留；
* 证据不足。

必须记录：

| Step | 业务指纹 | 文件/位置 | 当前内容 | 分类 | 判断依据 |
| ---- | ---- | ----- | ---- | -- | ---- |

只要任意一步存在不可解释的成熟业务残留，就不能判定九步清除全部通过。

## 八、检查是否误删公共逻辑

逐步检查清理前后是否保留：

* 正式入口；
* import 和依赖；
* 函数签名；
* 公共初始化；
* 公共登录、导航或环境准备；
* 通用采集器；
* 通用转换、标准化和比较逻辑；
* 公共输出逻辑；
* Schema；
* 日志和异常处理；
* 资源关闭；
* 步骤状态写入；
* 步骤之间的产物传递。

重点识别：

* 函数被清空后调用链中断；
* 变量或配置被删除后未定义；
* import 仍存在但目标实现被删；
* 公共函数被误判成业务代码；
* 上一步仍生成产物，但下一步读取路径被删；
* 输出格式被破坏；
* try/catch/finally 被破坏；
* 脚本语法成立但语义链路已失效。

## 九、检查步骤间依赖是否完整

验证九步之间的实际依赖关系：

```text
Step 1 输出 → Step 2 或后续使用
Step 2 输出 → Step 3 使用
……
Step 8 输出 → Step 9 使用
```

具体依赖必须以真实代码为准。

检查：

* 文件名和路径是否仍一致；
* JSON 结构是否保留；
* 必需字段是否被误删；
* Journey 业务值是否已清空但结构仍存在；
* 下游是否仍能读取清理后的占位产物或配置；
* 是否存在某一步清理后导致后续步骤无法启动；
* 是否误将公共 Schema 当作业务内容删除。

## 十、检查清理后是否存在明确填充位置

对 Step 1～Step 9 分别判断：

* 是否有明确业务填充函数；
* 是否有明确配置占位；
* 是否保留了函数壳；
* 是否保留必要参数；
* 是否只留下无意义空文件；
* 是否需要同时填代码和配置；
* 是否存在多个分散填充位置；
* 是否能让后续 Skill 精确定位修改范围。

输出：

| Step | 填充文件 | 填充函数/配置 | 当前状态 | 是否可直接填充 |
| ---- | ---- | ------- | ---- | ------- |

## 十一、检查当前清除实现

分析负责清除 Step 1～Step 9 业务内容的真实实现：

1. 清除入口；
2. 读取哪些文件；
3. 修改哪些文件；
4. 是否逐步分析 Step 1～Step 9；
5. 是否使用统一规则强行处理全部步骤；
6. 是否只按关键词删除；
7. 是否基于固定代码块或函数边界；
8. 是否检查跨文件业务内容；
9. 是否检查步骤间依赖；
10. 是否验证清理后的结构；
11. 是否扫描成熟模板残留；
12. 是否可能误删公共能力。

明确说明当前实现是否具备九步边界化清理能力。

## 十二、静态验证

使用仓库已有命令执行：

* Step 1～Step 9 脚本语法检查；
* lint；
* import 和引用检查；
* Schema 校验；
* 现有相关测试；
* 九步流程已有的 dry-run 或预扫描命令。

不得新增测试框架。

记录实际执行命令、结果和失败原因。

## 十三、运行边界验证

如环境允许，验证清理后的九步流程。

由于目标 Journey 业务尚未填充，不要求九步完整成功，但必须确认：

* 各步骤能否进入正式入口；
* 公共初始化是否正常；
* 是否能到达各自业务填充边界；
* 是否以明确、预期的方式停止；
* 是否错误执行成熟模板业务；
* 是否在错误状态继续生成产物；
* 是否因误删公共结构出现异常；
* 上一步未完成时，下游是否会正确阻止执行。

无法运行的项目必须标记为“未验证”，不得写成通过。

## 十四、通过标准

只有以下条件全部满足，才可判定九步业务清除通过：

* Step 1～Step 9 均已识别真实公共边界和业务边界；
* 无不可解释的成熟模板业务残留；
* 九步公共结构均完整保留；
* 步骤入口、函数、参数和 import 完整；
* 步骤之间的输入输出结构完整；
* 业务值已清除，但公共 Schema 未被破坏；
* 每一步都有明确的后续业务填充位置；
* 静态检查通过；
* 运行时能到达预期业务边界，或明确记录未验证；
* 未修改九步流程之外的无关内容；
* 当前清除实现具备可重复执行的明确边界。

## 十五、输出格式

### 1. 总体验收结论

只能选择：

* 通过，可以进入 Step 1～Step 9 业务填充；
* 部分通过，只允许填充明确通过的步骤；
* 不通过，必须先修正业务清除；
* 证据不足，暂不能进入业务填充。

### 2. 九步真实职责与调用链

| Step | 真实职责 | 入口 | 公共逻辑 | Journey 业务 | 输出 |
| ---- | ---- | -- | ---- | ---------- | -- |

### 3. 每一步业务指纹

分别列出 Step 1～Step 9 的模板业务内容。

### 4. 业务残留检查

| Step | 文件/位置 | 残留内容 | 分类 | 是否阻塞 |
| ---- | ----- | ---- | -- | ---- |

### 5. 公共代码保留检查

| Step | 公共能力 | 文件/函数 | 结果 | 证据 |
| ---- | ---- | ----- | -- | -- |

### 6. 步骤间依赖检查

列出九步之间的输入输出关系及当前完整性。

### 7. 填充位置检查

| Step | 填充位置 | 当前是否明确 | 是否可以直接填充 |
| ---- | ---- | ------ | -------- |

### 8. 当前清除实现评估

说明：

* 清除原理；
* 是否基于九步真实边界；
* 漏删风险；
* 误删风险；
* 是否可继续复用。

### 9. 静态和运行验证

列出实际命令、结果和未验证项。

### 10. 阻塞问题

只列阻止进入业务填充的问题。

### 11. 后续动作

明确说明：

* 是否需要修正清除实现；
* 哪些 Step 需要重新清除；
* 是否需要重新从成熟模板生成新 Journey；
* 哪些 Step 已经可以进入业务填充。

## 十六、禁止事项

* 不修改任何文件；
* 不直接修复清除实现；
* 不填充目标 Journey 业务；
* 不只分析 Step 1；
* 不假设九步使用相同清除边界；
* 不只检查 Journey 名称；
* 不把语法通过等同于业务清除成功；
* 不忽略配置、Schema、测试数据和步骤中间产物；
* 不在无法运行时声称验证通过；
* 不提出脱离现有实现的新架构。





请对成熟模板 Journey 和当前目标 Journey 的 Step 2 做只读实现分析，确定新系统 dev 环境下 Step 2 的业务填充方式。

## 一、任务背景

当前已经完成：

1. 从成熟模板 Journey 创建目标 Journey；
2. 清除目标 Journey Step 1～Step 9 中的成熟模板业务内容；
3. 清除结果已经完成验收；
4. 暂缓处理旧系统 Step 1；
5. 当前优先处理 Step 2。

目前人工理解是：

* Step 2 与新系统 dev 环境有关；
* Step 2 可能使用 Playwright 采集新系统 fields；
* 新 Journey 需要基于目标 Journey 的真实页面实现填充 Step 2。

以上仅作为分析线索，不得直接视为代码事实。

本次只分析，不修改任何文件。

## 二、任务目标

确认：

1. Step 2 的真实职责；
2. Step 2 的正式入口和调用链；
3. 新系统 dev 环境如何启动、访问和准备数据；
4. 哪些逻辑属于公共实现；
5. 哪些内容属于成熟模板 Journey 业务；
6. 清理后的目标 Journey Step 2 当前保留了什么；
7. 目标 Journey 的 Step 2 实际需要填充哪些代码、配置和数据；
8. 后续 Skill 应如何快速为不同 Journey 完成 Step 2。

## 三、分析范围

优先读取：

1. 成熟模板 Journey 的原始 Step 2；
2. 当前目标 Journey 清理后的 Step 2；
3. Step 2 正式执行入口；
4. Step 2 直接依赖的配置、helper、测试数据和采集器；
5. 新系统目标 Journey 的 React 页面代码；
6. 目标 Journey 使用的路由配置；
7. dev 环境相关配置；
8. stub、mock、fixture 或本地 JSON；
9. 已有 Step 2 边界分析或清理报告。

只读取与 Step 2 直接相关的范围，不分析 Step 1，也不展开 Step 3～Step 9。

## 四、确认 Step 2 的真实职责

请确认 Step 2 实际是否负责：

* 启动或连接新系统 dev 环境；
* 进入目标 Journey；
* 准备 dev 测试数据；
* 执行目标 Journey 页面操作；
* 采集 fields；
* 输出标准化结果；
* 或其他任务。

必须给出：

* 执行命令；
* 入口文件；
* 主函数；
* 输入；
* 输出；
* 下游使用方。

如果“Step 2 是新系统 dev 环境 fields 采集”与代码事实不完全一致，请明确修正。

## 五、还原真实调用链

从 Step 2 正式入口开始，还原实际调用关系，例如：

```text
Step 2 命令
→ Playwright 或执行入口
→ dev 环境检查
→ 路由或 Journey 导航
→ Journey 页面准备
→ Journey 页面内操作
→ fields 采集
→ 结果转换
→ 产物输出
```

以上只是待验证示例。

每一环必须标明：

* 文件路径；
* 函数；
* 配置；
* 调用方；
* 输入和输出。

## 六、确认 dev 环境运行方式

请查明：

1. dev 环境如何启动；
2. Step 2 是否负责启动 dev server，还是只连接已有服务；
3. 使用哪个 URL、端口和 base path；
4. 目标 Journey 的真实 route；
5. 是否经过微前端容器或宿主页；
6. 是否需要登录或模拟登录态；
7. 是否依赖环境变量；
8. 是否使用 stub、mock 或真实接口；
9. dev 数据从哪里读取；
10. 页面达到可操作状态的判断方式。

不得根据常规 React 项目经验推断。

## 七、分析目标 Journey 的页面实现

读取目标 Journey 在新系统中的真实页面代码，确认：

* 页面入口组件；
* route；
* 默认加载状态；
* 使用的公共组件；
* fields 所在区域；
* 页面加载需要的接口或数据；
* dev 环境下数据来源；
* 是否需要用户操作才显示目标 fields；
* 是否存在搜索、列表、详情、Tab、弹窗或编辑状态；
* 是否已有稳定的 `data-testid`、role、label 或其他定位信息；
* 哪些操作是真正达到 fields 采集页面所必需的。

不要修改 React 页面代码。

## 八、公共逻辑与 Journey 专属逻辑边界

请分类：

| 能力             | 公共实现 | Journey 专属 | 混合实现 | 未确认 |
| -------------- | ---- | ---------- | ---- | --- |
| dev 环境连接或启动    |      |            |      |     |
| 浏览器初始化         |      |            |      |     |
| 登录或身份准备        |      |            |      |     |
| 路由进入 Journey   |      |            |      |     |
| stub/mock 数据准备 |      |            |      |     |
| 页面加载等待         |      |            |      |     |
| 页面内业务操作        |      |            |      |     |
| fields 采集      |      |            |      |     |
| 结果输出           |      |            |      |     |
| 错误处理和资源关闭      |      |            |      |     |

每一项给出文件和函数证据。

## 九、成熟模板 Step 2 业务内容

按真实执行顺序列出成熟模板 Journey 的业务内容，包括：

* route；
* 页面文本；
* selector；
* 测试数据；
* stub/mock；
* 页面点击和输入；
* 查询或详情操作；
* 等待条件；
* 成功判断；
* fields 区域；
* 业务 helper；
* 输出标识。

输出：

| 顺序 | 文件/函数 | 业务内容 | 定位或数据来源 | 是否已从新 Journey 清除 |
| -- | ----- | ---- | ------- | ---------------- |

## 十、检查清理后的目标 Journey Step 2

确认：

1. 成熟模板业务是否已经清除；
2. 公共结构是否完整；
3. 当前是否保留明确的业务填充位置；
4. 是否存在未定义变量、失效 import 或空调用；
5. 是否仍保留 dev 环境、采集和输出公共逻辑；
6. 是否遗漏了必须同步填充的配置、stub 或测试数据；
7. 当前 Step 2 是否已经具备实施条件。

## 十一、确定目标 Journey 的真实填充内容

基于当前代码说明：

| 文件 | 函数/配置 | 需要填充的内容 | 信息来源 | 是否必须 |
| -- | ----- | ------- | ---- | ---- |

重点判断是否需要填充：

* Journey route；
* 页面内操作；
* dev 数据；
* stub/mock 配置；
* 测试数据；
* 页面等待；
* locator；
* fields 区域配置；
* Journey 标识；
* 输出路径或元数据。

不要预设所有项目都需要这些内容。

## 十二、评估快速填充方式

目标是未来每个 Journey 都可以快速制作 Step 2。

请基于现有实现判断，后续 Skill 最少需要接收什么输入，例如：

* Journey 名称；
* route；
* 页面内操作；
* dev 数据场景；
* 测试数据；
* 其他代码事实要求的信息。

同时说明：

* 哪些信息可以直接从新系统代码读取；
* 哪些信息可以从配置或 stub 读取；
* 哪些信息必须由用户提供；
* 哪些内容不应要求用户重复提供。

不得设计脱离现有代码的新 DSL 或新框架。

## 十三、验证方式

请给出当前仓库已有的最小验证方式：

* dev 环境启动或状态检查命令；
* Step 2 执行命令；
* 语法或 lint 检查；
* Playwright 相关测试；
* fields 产物路径；
* 如何确认采集的是目标 Journey 页面。

本次只列出并在安全、只读条件允许时执行现有检查，不修改代码。

## 十四、输出格式

### 1. Step 2 结论摘要

说明：

* Step 2 真实职责；
* dev 环境使用方式；
* 公共和 Journey 专属边界；
* 目标 Journey 的真实填充范围；
* 当前是否可以进入实施。

### 2. Step 2 真实调用链

列出文件、函数、配置和产物。

### 3. dev 环境事实

* 启动方式；
* URL 和端口；
* route；
* 数据来源；
* stub/mock；
* 登录或身份状态；
* 页面加载条件。

### 4. 公共与 Journey 专属边界

| 文件 | 函数/配置 | 职责 | 分类 | 后续是否允许修改 |
| -- | ----- | -- | -- | -------- |

### 5. 成熟模板业务与清理结果

列出已清除内容、残留和公共结构完整性。

### 6. 目标 Journey 页面事实

列出目标 Journey 的页面入口、组件、数据和必要操作。

### 7. Step 2 填充清单

| 文件 | 位置 | 填充内容 | 信息来源 |
| -- | -- | ---- | ---- |

### 8. 后续 Skill 最小输入

区分：

* 可从代码自动获取；
* 用户必须提供；
* 当前未确认。

### 9. 验证命令

列出现有命令和结果。

### 10. 最终结论

明确说明：

> 目标 Journey 的 Step 2 应修改哪些文件和位置、填入哪些新系统 dev 环境业务内容，以及是否可以进入实施阶段。

## 十五、禁止事项

* 不修改任何文件；
* 不填充 Step 2；
* 不处理 Step 1；
* 不分析 Step 3～Step 9；
* 不修改 React 页面；
* 不修改公共组件；
* 不重新设计 dev 环境；
* 不新增 mock、stub 或测试框架；
* 不把人工理解直接当成事实；
* 不从成熟模板复制业务操作；
* 不在缺乏代码证据时虚构 route、selector 或测试数据。
