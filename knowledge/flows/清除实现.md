当前清除业务代码的实现未基于 Step 1 的真实调用边界进行清理，现有实现判定为不可靠。

请停止继续进行目标 Journey 的 Step 1 业务填充，先修正“模板业务清除”能力。

## 一、当前问题

现有清除实现没有先分析成熟模板 Step 1 的：

* 正式执行入口；
* 完整调用链；
* 公共代码与 Journey 专属代码边界；
* 直接依赖的配置、测试数据和辅助函数；
* Journey 业务操作与 fields 采集之间的衔接关系。

因此，当前清除结果可能存在：

* 误删公共代码；
* 漏删成熟模板业务；
* 只清理主脚本、未清理关联配置和数据；
* 保留无效等待、selector 或页面判断；
* 破坏 Step 1 原有调用关系；
* 形成无法可靠填充的新 Journey 空壳。

不得继续假设当前清理结果正确。

## 二、本次任务目标

重新基于成熟模板 Step 1 的真实实现，完成以下工作：

1. 还原 Step 1 正式调用链。
2. 确定 Step 1 的公共代码边界。
3. 确定成熟模板 Journey 的业务代码边界。
4. 审计当前清除实现和清除结果。
5. 制定精确的边界化清除规则。
6. 修正清除实现。
7. 重新生成目标 Journey 的干净模板。
8. 验证清除后 Step 1 的结构和公共调用链仍然完整。

本次不要进行目标 Journey 的业务脚本填充。

## 三、第一阶段：只读分析成熟模板 Step 1

从正式执行入口开始，沿真实调用链确认：

* 启动命令；
* 入口文件；
* 主函数；
* Playwright 初始化；
* 登录；
* 导航；
* Journey 页面进入；
* Journey 页面内业务操作；
* 测试数据；
* 等待和页面判断；
* fields 采集；
* 截图和输出；
* 日志和异常处理；
* 资源关闭。

所有结论必须精确到：

* 文件路径；
* 函数名；
* 配置项；
* 调用方；
* 下游调用。

不得根据文件名、注释或当前人工理解直接判断职责。

## 四、确定真实边界

请将 Step 1 相关内容分为四类：

### 1. 公共固定代码

必须保留，清除过程不得修改，例如实际代码中已确认的：

* 登录；
* 公共导航；
* Playwright 初始化；
* 公共等待；
* fields 采集；
* 输出；
* 日志；
* 异常处理。

以上只是待验证范围，必须以代码事实为准。

### 2. 成熟 Journey 专属业务

必须清除或替换，例如：

* 页面内点击、输入、选择；
* Journey 专属 selector；
* Journey 页面文本和标题；
* Journey 专属测试数据；
* 表格、Tab、弹窗、iframe 操作；
* Journey 专属等待与成功判断；
* Journey 专属配置和辅助函数。

### 3. 公共与业务混合代码

不得直接整体删除。

必须说明：

* 公共部分是什么；
* 业务部分是什么；
* 应如何保留函数结构并只清除业务内容。

### 4. 无法确认的代码

不得自动删除。

应进入待确认清单。

## 五、审计当前清除实现

找到当前负责清除模板业务的脚本、Skill、Prompt 或代码，分析：

* 清除入口；
* 清除依据；
* 读取范围；
* 修改范围；
* 删除方式；
* 是否使用固定关键词；
* 是否使用固定文件列表；
* 是否理解调用链；
* 是否检查跨文件业务依赖；
* 是否验证清除后的可运行性；
* 是否扫描模板残留。

明确列出当前实现为什么不可靠。

## 六、制定边界化清除清单

基于真实调用链输出：

| 文件 | 函数/代码块 | 分类 | 清除动作 | 清除后应保留的结构 |
| -- | ------ | -- | ---- | --------- |

清除动作只能是：

* 完整保留；
* 删除业务语句但保留函数壳；
* 替换为明确占位；
* 重置 Journey 配置值；
* 删除 Journey 专属数据；
* 保留待确认，不自动处理。

不得再使用“扫描到类似业务代码就删除”的模糊规则。

## 七、修正清除实现

只有在分析和清除清单完成后，才能修改清除实现。

修正要求：

1. 清除范围必须基于确定的文件、函数或代码块边界。
2. 不得修改 Step 1 公共链路。
3. 不得删除公共函数调用。
4. 不得留下成熟模板 Journey 的名称、selector、数据和判断条件。
5. 对公共与业务混合函数，只清除业务段。
6. 清除后必须保留后续业务填充所需的明确插槽。
7. 不得修改 Step 2～Step 9。
8. 不得重构 Step 1。
9. 不得新增另一套模板机制。

## 八、重新生成并验证

修正清除实现后，重新从成熟模板创建一次目标 Journey。

必须验证：

### 静态验证

* 语法检查通过；
* import 和变量引用完整；
* 公共函数调用未丢失；
* Step 1 入口仍然完整；
* Journey 业务填充位置明确。

### 残留验证

扫描成熟模板 Journey 的：

* 名称；
* 菜单；
* URL；
* selector；
* 页面文本；
* 测试数据；
* 截图名称；
* 输出标识；
* 配置；
* 辅助函数。

残留项必须逐项解释，无法解释的残留视为清除失败。

### 边界验证

通过 diff 确认：

* 公共代码未被修改；
* 只清除了成熟 Journey 业务内容；
* 未影响 Step 2～Step 9；
* 未新增无关文件。

### 运行边界验证

如当前空模板因尚未填充业务而不能完整执行，不得将其判为失败。

但必须验证：

* 可以进入 Step 1 正式入口；
* 公共初始化链路完整；
* 到 Journey 业务插槽前没有因误删而报错；
* 失败位置符合“业务尚未填充”的预期。

## 九、输出产物

请生成正式报告，至少包含：

### 1. Step 1 真实调用链

### 2. 公共代码与 Journey 业务边界

### 3. 当前清除实现不可靠的原因

### 4. 原清除结果中的误删、漏删和结构风险

### 5. 边界化清除清单

### 6. 清除实现的修改内容

### 7. 重新生成结果

### 8. 静态、残留、边界和运行验证结果

### 9. 是否可以进入 Step 1 业务填充

最终结论只能是以下之一：

* 可以进入 Step 1 业务填充；
* 暂不可进入，仍存在明确问题。

## 十、禁止事项

* 不继续填充目标 Journey 业务脚本。
* 不把原清除结果作为正确基线。
* 不按关键词全局删除代码。
* 不只分析 Step 1 主文件。
* 不跳过配置、测试数据和辅助函数。
* 不修改 Step 2～Step 9。
* 不重构成熟模板。
* 不新增目录和新框架。
* 未完成调用边界分析前，不得修改清除实现。
