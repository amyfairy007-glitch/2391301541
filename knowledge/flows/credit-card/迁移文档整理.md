你现在要对当前仓库内所有“旧项目迁移到新项目”相关资料做一次完整的文档盘点、内容归类和规则收敛分析。

重要背景：
当前仓库中存在大量迁移相关文档、规则、提示词、采集说明、组件映射、API 规范、目录规范、验证说明、历史草稿和执行记录。这些内容目前混在一起，可能存在重复、过期、冲突、不完整、命名不统一等问题。现在需要先全面梳理：仓库里到底有哪些迁移相关内容、每份文档讲了什么、哪些规则仍然有效、哪些内容重复、哪些内容冲突、哪些内容应该沉淀为正式规范。

本次任务只做分析和文档治理，不做业务代码修改。

# 一、硬性限制

必须遵守：

1. 不要删除任何文件。
2. 不要移动任何文件。
3. 不要修改业务代码。
4. 不要修改项目顶层结构。
5. 不要批量重命名 journey / jounery 路径。
6. 不要擅自把旧规则改成最终规则。
7. 如果发现规则冲突，只记录冲突，不要擅自决定。
8. 如果无法判断某条规则是否有效，必须标记为“需要人工确认”。
9. 本轮重点是“盘点、分类、归纳、发现问题、提出候选规则”，不是直接重构整个文档体系。
10. 可以新增分析报告文档，输出到 docs/migration-audit/ 目录下。

# 二、扫描范围

请扫描整个仓库中所有可能与“旧项目迁移到新项目”相关的 md / txt / json / yaml / yml / README / AGENTS 文件。

重点扫描但不限于：

* docs/
* migration/
* migration-tool/
* prompts/
* rules/
* README.md
* AGENTS.md
* 所有包含迁移规则、迁移提示词、迁移流程、组件映射、Playwright 采集、API stub、journey 生成、Code Review、验收等内容的文件

请重点检索以下关键词：

* migration
* migrate
* 迁移
* 旧项目
* 新项目
* journey
* jounery
* Playwright
* 采集
* fields
* actions
* api
* validation
* screenshot
* screenshots
* stub
* mock
* dev
* sit
* buildrequest
* constants
* strings
* utils
* component mapping
* 组件映射
* 公共组件
* select
* table
* input
* modal
* CSS
* 样式
* Code Review
* 验收
* 自查
* 提示词
* prompt
* AGENTS

# 三、输出文件

请新增以下分析报告：

docs/migration-audit/01-migration-docs-content-inventory.md
docs/migration-audit/02-migration-knowledge-map.md
docs/migration-audit/03-candidate-final-rules.md
docs/migration-audit/04-conflicts-and-confirmations.md
docs/migration-audit/05-next-cleanup-plan.md

如果 docs/migration-audit/ 不存在，可以创建该目录。

不要修改原有迁移文档。

# 四、报告一：迁移文档内容盘点

输出路径：

docs/migration-audit/01-migration-docs-content-inventory.md

该报告用于回答：当前仓库里到底有哪些迁移相关文档，每份文档讲了什么。

必须包含以下内容：

## 1. 文档总览

统计并说明：

* 一共找到多少份疑似迁移相关文档
* 这些文档分布在哪些目录
* 文档类型有哪些，例如：

  * 正式规则
  * 临时草稿
  * AI 提示词
  * 迁移流程
  * Playwright 采集说明
  * 组件映射说明
  * API / stub 说明
  * 验证 / 验收说明
  * Code Review 说明
  * 执行结果报告
  * 历史记录
  * 无法归类内容

## 2. 每个文档的摘要表

用表格列出所有相关文档。

表格字段必须包含：

* 文件路径
* 文档类型
* 主要主题
* 主要内容摘要
* 提到的重要规则
* 是否像正式规则
* 是否像临时草稿
* 是否像执行结果
* 是否重复
* 是否疑似过期
* 是否存在冲突
* 建议处理方式：保留 / 合并 / 归档 / 废弃 / 需要人工确认
* 备注

## 3. 重复内容清单

列出哪些内容被多份文档重复提到。

重点检查：

* journey 目录规则是否重复
* buildrequest.js 职责是否重复
* constants.js / strings.js / utils.js 职责是否重复
* Playwright 采集规则是否重复
* fields / actions / api / validation 是否重复
* 组件映射规则是否重复
* dev / sit / stub 规则是否重复
* Code Review / 验收规则是否重复
* AI 提示词是否存在多个版本

每条重复内容需要包含：

* 重复主题
* 涉及文档
* 各文档说法是否一致
* 是否建议合并
* 建议合并到哪个正式主题

## 4. 疑似过期内容清单

列出明显像旧方案、临时方案、已被后续规则替代的内容。

重点检查：

* 是否还有文档要求采集 CSS
* 是否还有文档要求生成原生 select / table
* 是否还有文档允许请求参数直接写在 index.jsx
* 是否还有文档使用 jounery 拼写
* 是否还有文档要求修改顶层结构
* 是否还有文档与当前 src/journey、src/stub 规则不一致
* 是否还有过长、过旧、不可执行的提示词

每条过期内容需要包含：

* 文件路径
* 过期内容
* 为什么疑似过期
* 被哪个新规则或新方案替代
* 是否需要人工确认

# 五、报告二：迁移知识内容地图

输出路径：

docs/migration-audit/02-migration-knowledge-map.md

该报告用于把仓库里所有迁移知识重新按模块组织，让人能看懂“现在到底有哪些迁移资料”。

请按以下模块整理：

## 1. 文档治理类

整理：

* 哪些是正式规则
* 哪些是候选规则
* 哪些是临时草稿
* 哪些是历史记录
* 哪些是执行报告
* 哪些应该归档
* 哪些应该废弃
* 哪些需要人工确认

## 2. 输入资料类

整理一次迁移任务开始前需要读取或准备的资料。

重点检查是否存在以下内容：

* 旧系统采集结果
* 页面截图
* fields
* actions
* api
* validation
* 接口白名单
* request / response 数据
* 组件映射资料
* 公共组件 README
* 已迁移成功的 journey 样例
* 旧系统页面地址
* 新系统目标 journey 名称

输出：

* 已有规则
* 来源文档
* 是否完整
* 是否需要补充

## 3. 目录结构类

整理所有目录结构相关规则。

重点包括：

* src/
* src/journey/
* src/stub/
* docs/
* migration/
* migration-audit/
* journey 与 stub 的相对关系
* 是否允许修改顶层结构
* journey / jounery 拼写混用
* 每个 journey 下应该有哪些文件

输出：

* 当前已有说法
* 一致的部分
* 不一致的部分
* 推荐候选规则
* 是否需要人工确认

## 4. 代码生成规则类

整理每个生成文件的职责。

必须覆盖：

* index.jsx 职责
* buildrequest.js 职责
* constants.js 职责
* strings.js 职责
* utils.js 职责
* 页面状态管理规则
* 表单处理规则
* 查询区处理规则
* 表格处理规则
* 弹窗处理规则
* 按钮事件处理规则
* 页面初始化规则

重点检查：

* 请求参数是否必须集中到 buildrequest.js
* 硬编码文案是否必须进入 strings.js
* 公共常量是否必须进入 constants.js
* 页面通用函数是否必须进入 utils.js
* index.jsx 是否过胖
* 是否允许在页面内直接写复杂参数拼接

## 5. API / stub / 环境类

整理：

* dev / sit 环境规则
* stub/test 数据规则
* 接口命名规则
* mock 数据保存路径
* request / response 保存规则
* buildrequest.js 与 API 的关系
* 是否允许页面直接拼请求参数
* 接口白名单规则
* 错误处理规则
* 接口返回数据适配规则

输出：

* 已有规则
* 来源文档
* 冲突点
* 候选最终规则
* 需要人工确认点

## 6. Playwright 采集类

整理旧系统采集相关内容。

必须覆盖：

* 采集入口
* 手动操作流程
* 自动采集流程
* fields 采集
* actions 采集
* api 采集
* validation 采集
* screenshots 采集
* 接口白名单
* request / response 保存
* 是否采集 CSS
* 采集产物保存路径
* 采集报告格式
* 采集数据如何供 AI 迁移使用

重点检查：

* 是否存在“采集 CSS”和“不采集 CSS”的冲突
* 是否存在采集数据过多导致噪音的问题
* 是否存在只采集白名单接口的规则
* 是否存在隐藏页面 / 弹窗 / 多步骤截图的采集规则

## 7. 组件映射类

整理旧系统控件到新系统公共组件的映射规则。

必须覆盖：

* select
* table
* input
* button
* modal
* form
* search area
* pagination
* date picker
* upload
* layout component

重点检查：

* 是否禁止生成原生 select / table / modal
* 是否要求读取公共组件 README
* 是否有 component mapping 文档
* 是否有成功样例
* 映射不确定时如何处理
* 是否存在两套公共组件库
* 是否存在组件用法抽取规则

## 8. 样式和布局类

整理：

* 是否保留旧 CSS
* 是否采集 CSS
* 是否允许生成页面级 CSS
* 是否使用公共布局组件
* 是否要求视觉还原
* 是否基于截图做结构还原
* 是否允许局部样式补充
* 旧系统 DOM 是否需要复刻

重点检查：

* “不采集 CSS，布局交给公共组件”是否是候选最终规则
* 是否存在旧文档要求复制旧 CSS
* 是否存在 AI 生成大量无用 CSS 的问题记录

## 9. 验证和验收类

整理：

* 页面行为核对
* 字段核对
* validation 核对
* API 请求核对
* request 参数核对
* response mock 核对
* stub 验证
* dev / sit 验证
* Code Review
* 自查清单
* 验收报告
* 迁移完成标准

重点检查：

* 是否有固定 Code Review 报告要求
* 是否有迁移完成后的自查要求
* 是否有人工验证页面行为的规则
* 是否有“进入页面请求哪些、点击按钮请求哪些”的核对规则

## 10. AI 执行提示词类

整理所有给 Codex / OpenCode / AI 使用的迁移提示词。

必须分析：

* 哪些提示词是旧版
* 哪些提示词接近最终版
* 哪些提示词过长
* 哪些提示词适合直接复用
* 哪些提示词只是阶段性草稿
* 提示词是否包含输入资料
* 提示词是否包含输出产物
* 提示词是否包含禁止事项
* 提示词是否包含自查清单
* 提示词是否要求读取现有项目事实
* 提示词是否容易让 AI 乱改结构

## 11. 成功样例类

尝试识别仓库中是否存在已经迁移成功或较规范的 journey / 页面 / 文档。

重点检查：

* 哪些 journey 可以作为模板
* 哪些文件结构接近最终规范
* 哪些规则已经在成功样例里体现
* 哪些样例可以作为后续 AI 生成的参考
* 是否有迁移结果报告能证明样例有效

注意：
如果无法确认某个 journey 是否成功，不要擅自判断，只标记为“疑似成功样例 / 需要人工确认”。

## 12. 问题反例类

整理文档中记录过的失败经验、踩坑和坏味道。

重点检查：

* index.jsx 太胖
* 请求参数散落在页面里
* 原生 select / table 未替换
* CSS 过多
* 硬编码文案过多
* constants.js / strings.js 边界混乱
* utils.js 放入过多业务逻辑
* stub 命名不一致
* 组件用法不符合 README
* AI 未读取现有项目结构
* AI 擅自改顶层目录
* AI 生成内容无法运行
* JSON 过大且噪音过多
* 迁移报告不可读

输出：
这些问题应该进入后续自查清单。

## 13. 命名规范类

整理所有命名相关规则。

必须覆盖：

* journey 命名
* jounery / journey 拼写统一建议
* 文件命名
* 接口命名
* stub 文件命名
* 常量命名
* strings key 命名
* 工具函数命名
* 组件命名
* 目录命名
* 路由命名

如果发现命名规则不完整，请标记为“需要补充”。

## 14. 后续维护类

整理迁移文档后续如何维护。

重点包括：

* 新规则应该写到哪里
* 旧规则如何归档
* AI 提示词如何更新
* 组件映射如何更新
* 每次迁移结果如何归档
* Code Review 报告放哪里
* 迁移规范变更如何记录
* 如何避免文档再次变乱

# 六、报告三：候选最终规则

输出路径：

docs/migration-audit/03-candidate-final-rules.md

注意：
这里不是正式定稿，而是“候选最终规则”。如果证据不足或存在冲突，必须标记为“需要人工确认”。

请按以下结构输出：

# 候选最终迁移规则

## 1. 目录结构候选规则

列出当前最可能应该保留的目录结构规则。

必须说明：

* 规则内容
* 来源文档
* 支持理由
* 是否存在冲突
* 是否需要人工确认

## 2. journey 生成候选规则

整理：

* journey 应放在哪里
* 每个 journey 应包含哪些文件
* 每个文件职责是什么
* journey 与 stub 如何对应
* 是否统一使用 journey 拼写

## 3. 文件职责候选规则

分别整理：

* index.jsx
* buildrequest.js
* constants.js
* strings.js
* utils.js

每个文件都要说明：

* 应该放什么
* 不应该放什么
* 常见违规情况
* 来源文档

## 4. API / stub 候选规则

整理：

* dev / sit
* stub/test
* 接口命名
* request 参数
* response mock
* buildrequest.js
* 接口白名单

## 5. Playwright 采集候选规则

整理：

* 采集什么
* 不采集什么
* fields / actions / api / validation 的作用
* 截图如何使用
* 是否采集 CSS
* 白名单接口规则

## 6. 组件映射候选规则

整理：

* 原生控件是否允许
* 公共组件优先级
* 组件 README 使用规则
* select / table / input / modal 等映射规则
* 映射不确定时如何处理

## 7. 样式布局候选规则

整理：

* 是否保留旧 CSS
* 是否采集 CSS
* 是否使用公共布局组件
* 是否允许页面级 CSS
* 截图如何辅助布局还原

## 8. 验证验收候选规则

整理：

* 页面行为核对
* API 请求核对
* 字段核对
* validation 核对
* Code Review
* 自查报告
* 验收报告

## 9. AI 执行候选规则

整理：

* AI 执行迁移前必须读取哪些资料
* AI 必须输出哪些文件
* AI 禁止做什么
* AI 完成后必须自查什么
* AI 遇到冲突时如何处理

## 10. 后续维护候选规则

整理：

* 正式规则放哪里
* 历史文档如何归档
* 组件映射如何更新
* 提示词如何更新
* 每次迁移产物如何归档

# 七、报告四：冲突与人工确认清单

输出路径：

docs/migration-audit/04-conflicts-and-confirmations.md

该报告只记录冲突和需要人工确认的问题。

必须包含以下表格：

## 1. 规则冲突清单

表格字段：

* 冲突编号
* 冲突主题
* 规则 A
* 来源文档 A
* 规则 B
* 来源文档 B
* 影响范围
* 推荐处理方式
* 是否需要人工确认
* 备注

重点检查以下冲突：

1. 是否采集 CSS
2. 是否复制旧 CSS
3. 是否允许生成原生 select / table / modal
4. journey / jounery 拼写
5. journey 路径
6. stub 路径
7. 请求参数是否允许写在 index.jsx
8. buildrequest.js 是否必须存在
9. strings.js / constants.js / utils.js 边界
10. dev / sit / stub 规则
11. Playwright 采集范围
12. 组件映射来源
13. Code Review 是否强制
14. 是否允许修改顶层结构
15. AI 是否可以自动删除或移动旧文档

## 2. 需要人工确认的问题

表格字段：

* 问题编号
* 问题描述
* 涉及文档
* 可能选项
* 推荐选项
* 推荐原因
* 不确认会有什么风险

# 八、报告五：下一步清理计划

输出路径：

docs/migration-audit/05-next-cleanup-plan.md

该报告用于提出后续如何把这些乱文档收敛成正式文档。

必须包含：

## 1. 当前迁移文档主要乱在哪里

例如：

* 文档太多
* 多个提示词版本并存
* 旧规则和新规则混在一起
* 目录路径不一致
* journey / jounery 拼写混用
* Playwright 采集规则多次变化
* 组件映射规则不集中
* API / stub 规则分散
* 验收规则不固定

## 2. 哪些主题已经比较稳定

列出稳定主题及理由。

## 3. 哪些主题最需要人工确认

列出高优先级确认项。

## 4. 建议最终沉淀的正式文档结构

建议最终整理成：

docs/migration/
00-index.md
01-final-rules.md
02-ai-execution-prompt.md
03-standard-workflow.md
04-cleanup-inventory.md

并说明每份文档用途：

* 00-index.md：迁移文档入口，告诉人和 AI 应该看哪些正式文档
* 01-final-rules.md：最终迁移规则，给人看
* 02-ai-execution-prompt.md：给 Codex / OpenCode 执行迁移用
* 03-standard-workflow.md：标准迁移流程
* 04-cleanup-inventory.md：旧文档处理清单

## 5. 建议归档策略

说明旧文档如何处理：

* 哪些直接保留
* 哪些合并后归档
* 哪些只作为历史参考
* 哪些疑似废弃但需要人工确认
* 不要删除旧文档，先归档

## 6. 建议执行顺序

给出后续执行顺序：

1. 先看 01-migration-docs-content-inventory.md
2. 再看 04-conflicts-and-confirmations.md
3. 人工确认关键冲突
4. 再生成正式 docs/migration/ 文档
5. 最后再考虑归档旧文档
6. 全程不删除业务代码

# 九、最终回复要求

完成后，请在终端回复中简要说明：

1. 新增了哪些报告文件
2. 一共扫描到多少份迁移相关文档
3. 发现了多少个主要冲突
4. 发现了多少个需要人工确认的问题
5. 哪些主题最混乱
6. 哪些主题已经比较稳定
7. 下一步建议先看哪份报告

再次强调：
本轮只做盘点、归类、分析、候选规则提炼，不要删除、移动、重命名旧文件，不要修改业务代码，不要直接定稿最终规范。




# 十、迁移
确认进入正式只读审计，不需要再输出“执行版审计计划表”。

请按现有预扫描结果继续执行文档审计，并落盘生成正式报告。

要求：

1. 不修改业务代码。
2. 不删除、移动、重命名任何旧文档。
3. 不整理成熟库 / AI库对比资料，只作为相关参考资料标记。
4. 不再新增中间计划表。
5. 直接输出以下审计报告：

docs/migration-audit/01-migration-docs-content-inventory.md
docs/migration-audit/02-migration-knowledge-map.md
docs/migration-audit/03-candidate-final-rules.md
docs/migration-audit/04-conflicts-and-confirmations.md
docs/migration-audit/05-next-cleanup-plan.md

重点审计顺序按你预扫描的优先级执行：

P0：
优先审 migration.md、00/01/08/10 等核心规范文档，以及明显属于 baseline / alignment / migration 的文档。

P1：
审页面迁移、组件映射、API、校验、buildRequest、dev/sit/mock/stub、Playwright采集相关文档。

P2：
审历史参考、旧提示词、skills 中疑似过期或重复的迁移规则。

重点识别并记录：

- 规范来源重复
- 组件映射规则冲突
- 样式 / CSS 规则冲突
- dev / sit / mock / stub 边界不清
- Playwright 采集边界不清
- buildRequest 职责不一致
- validation / API / 页面行为验收规则不一致
- journey / jounery 命名混用
- 正式规则、历史草稿、执行结果报告混在一起的问题

输出完成后，只在终端总结：

1. 实际审计文档数量
2. 生成了哪些报告
3. 发现的主要冲突数量
4. 需要人工确认的问题数量
5. 最混乱的前三类主题
6. 最稳定的前三类主题
7. 下一步建议先看哪一份报告